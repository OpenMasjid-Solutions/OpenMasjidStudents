# SPDX-License-Identifier: AGPL-3.0-only
# Copyright (C) 2026 OpenMasjid-Solutions
#
# Contributor License Agreement enforcement (CLA Assistant bot).
#
# On every pull request the bot checks whether each contributor has signed CLA.md.
# If not, it comments asking them to reply with the exact sentence below; they sign
# ONCE and are remembered thereafter.
#
# Signatures are stored on a DEDICATED branch (`cla-signatures`), NOT on the default
# branch. The reason is branch protection: where `main` requires the "cla" status
# check, the bot cannot commit the signatures file to it (that fails with
# `Required status check "cla" is expected`). A separate storage branch keeps signing
# working regardless. The built-in GITHUB_TOKEN can write it; PERSONAL_ACCESS_TOKEN is
# used when present (optional).
#
# NOTE (2026-08-04 audit): this comment used to assert that `main` IS protected with a
# required "cla" check. It is not — `GET /repos/.../branches/main/protection` answers
# 404 "Branch not protected", so a direct push to main currently succeeds and nothing
# requires any check to pass first. Enabling that protection is recommended and needs a
# repo admin; see docs/audit/ACTION_REQUIRED.md. Keeping the storage branch separate is
# correct either way, and becomes load-bearing the moment protection is turned on.
#
# `actions: write` is REQUIRED (this is the official contributor-assistant setup):
# after a contributor posts the sign comment, the action RE-RUNS the PR's workflow to
# flip the required `cla` check from red to green. Without it the signature is
# recorded but the check stays red ("Resource not accessible by integration"). It is
# safe here because this workflow runs ONLY the pinned action and never checks out or
# executes the PR's code — the usual pull_request_target risk.
name: CLA Assistant

on:
  issue_comment:
    types: [created]
  pull_request_target:
    types: [opened, synchronize]

permissions:
  actions: write
  contents: write
  pull-requests: write
  statuses: write

jobs:
  cla:
    runs-on: ubuntu-latest
    steps:
      - name: CLA Assistant
        if: >
          github.event.comment.body == 'recheck'
          || github.event.comment.body == 'I have read the CLA Document and I hereby sign the CLA'
          || github.event_name == 'pull_request_target'
        uses: contributor-assistant/github-action@ca4a40a7d1004f18d9960b404b97e5f30a505a08 # v2.6.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Optional; used when set. GITHUB_TOKEN alone is enough to store signatures
          # on the unprotected cla-signatures branch.
          PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        with:
          path-to-signatures: 'signatures/version1/cla.json'
          path-to-document: 'https://github.com/OpenMasjid-Solutions/OpenMasjidStudents/blob/main/CLA.md'
          # Dedicated unprotected branch that holds the signatures file (see header).
          branch: 'cla-signatures'
          # The custom sentence a contributor posts to sign:
          custom-pr-sign-comment: 'I have read the CLA Document and I hereby sign the CLA'
          custom-allsigned-prcomment: 'All contributors have signed the CLA. Thank you! 🕌'
          # Accounts that never need to sign (the maintainers + bots):
          allowlist: hasan-ismail,dependabot[bot],github-actions[bot]
