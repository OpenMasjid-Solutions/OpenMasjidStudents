node_modules/
dist/
packages/*/dist/
packages/server/public/

# Local data volume (the SQLite DB and its 30-minute snapshots — minors' PII and every payment record)
data/
packages/server/data/

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
Thumbs.db
*.swp
.vscode/settings.json
.idea/

# Env / secrets
.env
.env.local
.env.*.local

# Claude Code local settings (keep project settings, ignore local overrides)
.claude/settings.local.json
